<div class="shopping-cart section">
    <div class="container">



    @if (TempData["total"] == null)
    {
       
                        <h1>cart is empty</h1>
          





    }
    else
    {


        <div class="row">
            <div class="col-12">
                <!-- Shopping Summery -->
                <table class="table shopping-summery">
                    <thead>
                        <tr class="main-hading">
                            <th>PRODUCT</th>
                            <th>NAME</th>
                            <th class="text-center">BID PRICE</th>

                            <th class="text-center">TOTAL</th>
                            <th class="text-center"><i class="ti-trash remove-icon"></i></th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in TempData["cart"] as List<eBidWEBSITE.Models.Cart>)
                        {

                            <tr>
                                <td class="image" data-title="No"><img src="~/bidsUploads/@item.bid_image" alt="#"></td>
                                <td class="product-des" data-title="Description">
                                    <p class="product-name"><a href="#">@item.bid_Name</a></p>

                                </td>
                                <td class="price" data-title="bid Price"><span>$@item.bid_MINamount </span></td>

                                <td class="total-amount" data-title="Your bid"><span>$@item.bidding</span></td>
                                <td class="action" data-title="Remove"><a href="#"><i class="ti-trash remove-icon"></i></a></td>
                            </tr>

                                            <tr></tr>
                        }
                    </tbody>
                </table>
                <!--/ End Shopping Summery -->
            </div>
        </div>
    }






        @if (!User.Identity.IsAuthenticated)
            {
            <div class="col-lg-8 col-md-5 col-12">
                <div class="left">
                    <a href="@Url.Action("Login","Account")" class="btn btn-primary py-3 px-4">Login</a>
                    <a href="@Url.Action("Register","Account")" class="btn btn-primary py-3 px-4">Register</a>

                </div>
            </div>
        }



@if (User.Identity.IsAuthenticated)
{
    if (User.IsInRole("2"))
    {

        if (TempData["cart"] != null)
        {
            using (Html.BeginForm())
            {
                        <div class="row">
                            <div class="col-12">
                                <!-- Total Amount -->
                                <div class="total-amount">

                                    <div class="row">
                                    <div class="col-lg-8 col-md-5 col-12">
                                     <div class="left">
                                           </div>
                                        </div>



                                    
                                   
          




                                        <div class="col-lg-4 col-md-7 col-12">
                                            <div class="right">
                                                <ul>
                                                    <li>Cart Subtotal<span>$330.00</span></li>
                                                    <li>Shipping<span>Free</span></li>

                                                    <li class="last">You Pay<span>$310.00</span></li>
                                                </ul>
                                                <div class="button5">
                                                    <input type="submit" class="btn btn-dark" value="Checkout" >

                                                </div>
                                            </div>
                                        </div>
                                      </div>
            
                                </div>
                                <!--/ End Total Amount -->
                            </div>
                        </div>
            }
        }
    }
}













                    </div>
        </div>

